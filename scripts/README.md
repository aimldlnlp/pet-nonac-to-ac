# Helper Scripts

This folder contains supporting utilities that are not part of the main pipeline.

Current contents:

- `select_best_visual.py`: selects the best visual run from multiple candidates based on summary metrics
- `make_videos.py`: creates video assets from evaluation and visualization outputs

Scripts in this folder are optional and are primarily intended for presentation-oriented analysis or result curation.
